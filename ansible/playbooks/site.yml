---
# Main site playbook: Complete deployment workflow
# Combines genesis generation and node deployment

- name: Generate Genesis Files
  import_playbook: genesis.yml
  when: generate_genesis | default(true) | bool

- name: Deploy Nodes
  import_playbook: deploy-nodes.yml
  vars:
    node_names: "{{ node_names | default('all') }}"

